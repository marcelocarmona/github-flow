version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout # special step to check out source code to working directory
      - run:
          name: update-npm
          command: npm install
      - run:
          name: webpack build
          command: npm start
      - run:
          name: :)
          command: echo HEEEEE!!!!!!!
